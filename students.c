#include <stdio.h>
#include <stdlib.h>
#include <string.h>


struct student{
	char* fname;
	char* lname;
	float grade;
};

typedef struct student Student;

void insert_student(Student *x){

	printf("Insert first name, last name, and the grade of a student \n");
	scanf("%s %s %f", &x->fname, &x->lname, &x->grade);

	studentslist = (Student *)malloc(n
	


}


int main(){

	Student *studentslist;

	studentslist = (Student *)malloc(n
	
