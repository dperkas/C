#include <stdio.h>

int power(int base, int n){
	int i,p;
	for(i=p=1;i<=n;i++)
		p=p*base;
	return p;
}

int main(){
	int x, y,n,res;
	int power(int base, int n);
	x=1; y=1;

	int a,b;
	for(a,b=1;a<=n;a++)
		x=2*y;
	return x,y;
	res=power(x,y);
	printf("%d \n",res);

	return 0;
}